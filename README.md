# Hospital-Queue-Management-System-
 Hospital Queue Management System using an array. It allows adding patients (enqueue), serving or removing them (dequeue), and showing all waiting patients. The system uses front and rear to track positions and ensures patients are handled in first-come, first-served order.
